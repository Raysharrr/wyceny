/**
 * App chrome for `/valuations`, `/valuations/new`, `/valuations/[id]`
 * (Task 3). Profil/Wyloguj moved into the avatar dropdown in the Topbar
 * (Task 15) — the session-fetch + Topbar wiring itself now lives in the
 * shared `AppShellLayout`, reused as-is by `profile/layout.tsx`.
 */
export { AppShellLayout as default } from "@/components/app-shell-layout";
