{
  "name": "wyceny-app",
  "version": "0.1.0",
  "private": true,
  "description": "wyceny — property valuation app (monorepo root)",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel",
    "depcruise": "depcruise --config .dependency-cruiser.cjs apps/web/src packages/shared/src",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "lefthook install || true"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.29.3",
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0",
    "dependency-cruiser": "^18.0.0",
    "lefthook": "^2.1.9",
    "prettier": "^3.9.4",
    "turbo": "^2.10.2"
  },
  "pnpm": {
    "overrides": {
      "zod": "4.4.3"
    }
  }
}
