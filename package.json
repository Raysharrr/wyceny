{
  "name": "wyceny-app",
  "version": "0.1.0",
  "private": true,
  "description": "wyceny — property valuation app (monorepo root)",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.29.3",
  "devDependencies": {
    "turbo": "^2.10.2"
  },
  "pnpm": {
    "overrides": {
      "zod": "4.4.3"
    }
  }
}
