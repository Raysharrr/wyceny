# Handoff — Slice 11: UI wizard — parity z makietą v3-r4 (FR-13) — start od S1 brainstorm

Wklej poniższy blok w **nowej sesji Claude Code** uruchomionej w `/Users/michalczekala/Development/wyceny` (wiki repo — CLAUDE.md kontrakt + skille). Handoff STARTOWY: slice ma **zwalidowaną makietę** (UAT runda 2) i zero zewnętrznych API — **bez spike'a**; to slice czysto UI/web-only (worker nietykany, zero DDL — chyba że brainstorm zmieni model prowenancji, patrz rozstrzygnięcie 3). Sesja prowadzi pełny cykl `/build-slice`.

---

Poprowadź **Slice 11 — UI wizard: parity z makietą v3-r4 (FR-13)** przez pełny cykl `/build-slice`: S1 brainstorm (⛔ checkpoint zakresu) → S2 spec+plan (⛔ checkpoint planu, advisor-review przed) → S3 `superpowers:subagent-driven-development` (świeży implementer + niezależny reviewer per task, commit+push+CI per task, ledger `.superpowers/sdd/progress.md` — NOWA sekcja `# SLICE 11`, briefy `wizard-task-N-brief.md`/`-report.md`) → S4 CI/fitness → S5 deploy (⛔) → S6 wiki docs (PR, merge robi user).

**NAJPIERW:** potwierdź, że wiki-PR **#15 jest ZMERGOWANY** (squash `b2d12a7`, 2026-07-22) i `wiki/roadmap.md` NOW = „UI wizard: parity z makietą v3-r4 (FR-13)". Jeśli NOW jest inne — STOP, zapytaj usera.

**KONTEKST DO PRZECZYTANIA (w tej kolejności):**

1. `wiki/roadmap.md` — blok NOW (outcome: 7-krokowy wizard z gatingiem kroków, per-pole badge prowenancji + panel „Skąd te dane", ekran rozbieżności KW↔EGiB — trzy elementy makiety bez właściciela; `Should`; DoD = wizard na prodzie parity z makietą, zero regresji F-1/F-4).
2. **Makieta (źródło prawdy UI — reguła `references/ui-planning.md` skilla build-slice: NIE wymyślaj UI, przenoś makietę):**
   - klikalna: `raw/interactive-mockup/Wyceny - Makieta MVP (standalone) - v3-r4-2026-06-30.html` (1,9 MB standalone — otwórz w przeglądarce, NIE greppuj);
   - kod JSX: `raw/interactive-mockup/Wyceny - v2 - full code/` — `shared.jsx` (tablica `STEPS` L146–153: **1. Przedmiot · 2. Oględziny · 3. Próba · 4. Cechy · 5. Kalkulacja · 6. Opisy · 7. Operat**; `<Stepper current maxReached>` — makieta ma gating MIĘKKI przez `maxReached`; `Field`/`SourceTag` = wzorzec per-pole badge 🤖/✍️), `app.jsx` (router), `screens-*.jsx`, `tweaks-panel.jsx`, `data.js`. **UWAGA:** folder „Wyceny - v3 - full code" to ślepa odnoga (regres sprzed r4) — ignoruj;
   - `wiki/topics/product/makieta-v3-r4-2026-06-30.md` (podsumowanie r4).
3. `wiki/deliverables/2026-06-30-prd-mvp-wyceny.md` — **UWAGA na pułapkę etykiety:** FR-13 (L73) literalnie = „usuń rusztowanie edukacyjne makiety + sekcja Pomoc ODŁOŻONA na później"; twardy wymóg wizarda siedzi w rdzeniu FR (L57: „7-krokowy workflow z gatingiem, krok N+1 dostępny po zatwierdzeniu N") i **AC-5** (L81: „krok N niezatwierdzony → dostęp do N+1 zablokowany"). Do tego L110: wzorzec `Sourced<T>` „niesiony w typach **i** w UI (badge/field)" per pole. Roadmapowe „FR-13" to parasol na tę trójkę z gap analysis.
4. `wiki/topics/product/mvp-gap-analysis-2026-07-18.md` (L72–80) — skąd wzięła się trójka bez właściciela i dlaczego „po oględzinach" (zdjęcia wymuszają strukturę kroków).
5. `wiki/topics/tech/subject-data-egib-mpzp-slice.md` — known limitations Slice 5 (per-pole badge, „Skąd te dane", rozbieżności KW↔EGiB = jawnie odłożone do tego slice'a).
6. App-repo `~/Development/wyceny-app` (CodeGraph zindeksowany — `codegraph explore` zamiast grep) — obecny stan UI:
   - `apps/web/src/app/valuations/new/new-valuation-form.tsx` (**881 linii** — jeden długi formularz client-side; create wyceny NA KOŃCU po submit; guardy `fetchSeq`/`kwSeq`/`mapSeq`; `areaMismatch` L229 = jedyny dzisiejszy nudge rozbieżności, powierzchnia formularz↔dokument KW),
   - `apps/web/src/app/valuations/new/subject-section.tsx` (375 l.) + `kw-section.tsx` (288 l.) — sekcje w flow TWORZENIA; `apps/web/src/app/valuations/[id]/inspection-section.tsx` (183 l., Slice 10) — w flow DETALU. **Asymetria do rozwiązania w wizardzie** (krok 2 = Oględziny, a dziś zdjęcia wymagają istniejącego `valuationId`!),
   - `apps/web/src/app/valuations/[id]/page.tsx` (**694 l.** server component) — płaski pionowy stos kart: podsumowanie → KcsBreakdown → ComparablesProvenance → FeaturesCard → SubjectCard → KwCard → InspectionSection → blockery+akcje → PDF; badge prowenancji **grupowe** (`GroupProvenanceBadge` L172: EGiB/MPZP/KW/wagi; `ProvenanceBadge` L151 tylko per-wiersz RCN),
   - shadcn/ui zainstalowane (9): badge, button, card, checkbox, field, input, label, separator, table — **brak** tabs/dialog/popover/tooltip/accordion (doinstalowanie przez `pnpm dlx shadcn@latest add` w plan-tasku; framework API przez context7),
   - domena prowenancji: `apps/web/src/domain/provenance.ts` (`approvalGate` — F-4, inwariant SERWEROWY), `apps/web/src/lib/assign-provenance.ts`.

**ROZSTRZYGNIĘCIA DO BRAINSTORMU (S1, z userem — jedna kwestia na raz):**

1. **Gating: twardy (AC-5) vs miękki (makieta).** AC-5 mówi „krok N niezatwierdzony → N+1 zablokowany"; makieta implementuje miękkie `maxReached` (wolna nawigacja wstecz, do przodu do najdalszego osiągniętego). Napięcie PRD↔makieta do rozstrzygnięcia PRZED wszystkim innym — determinuje model kroków. Pamiętaj: **F-4 `approvalGate` pozostaje inwariantem serwerowym niezależnie od gatingu UI** (wizard to UX, nie brama).
2. **Architektura kroków i lifecycle szkicu.** Dziś wycena powstaje NA KOŃCU formularza (create z kompletem danych); wizard z krokiem 2 = Oględziny (upload wymaga `valuationId`!) wymusza WCZEŚNIEJSZE create — kiedy powstaje szkic (po kroku 1 „Przedmiot"?), co z pustymi szkicami (porzucone kroki 1/7), jak wygląda „wróć i edytuj" (dziś draft-mutacje to tylko confirm-akcje + inspection; edycja próby/cech po create NIE istnieje). Routing: searchParams `?step=N` vs nested routes; ile z 881-liniowego formularza da się przenieść per krok bez przepisywania (dekompozycja, nie rewrite — sekcje subject/kw już są komponentami).
3. **Per-pole badge prowenancji.** PRD L110 chce `Sourced<T>` per pole w UI, ale `inputs.provenance` jest dziś GRUPOWE (ewidencja/mpzp/kw/weights/geocode/area). Rekomendacja do przedyskutowania: per-pole badge w UI mapowane z grup (pole→grupa; zero zmian modelu/DDL, zero migracji prowenancji) vs rozszerzenie modelu na per-pole statusy (dotyka F-4/F-5/confirm-akcji — DUŻO większy zasięg). Makieta: `Field`/`SourceTag` 🤖/✍️ w `shared.jsx`.
4. **Panel „Skąd te dane".** Kształt z makiety (`tweaks-panel.jsx`?), zawartość: source+status per grupa/pole, `fetchedAt`/meta (sampleMeta/subjectMeta/kwMeta już są w inputs), linki do źródeł (geoportal/RCN)? Gdzie żyje (sidebar/popover per pole/osobny krok 5 „Kalkulacja")?
5. **Ekran rozbieżności KW↔EGiB.** Co dokładnie pokazuje makieta (sprawdź w klikalnej); co REALNIE porównywalne dziś: pow. użytkowa (KW/akt vs formularz — nudge istnieje), nr KW (slot lokal/grunt vs pole ręczne), adres/działka (EGiB vs KW dział I-O — ekstrakt NIE niesie adresu z działu I-O w schemacie? sprawdź `kw-snapshot`). Czy ekran blokuje (twardy stop) czy informuje (nudge-pattern — precedens: „nudge, never a block")?
6. **FR-13 sensu stricto (rusztowanie edukacyjne).** Co w DZISIEJSZYM produkcie jest rusztowaniem do usunięcia, a co zostaje (rozbicie T2/T3/T4 „zero czarnej skrzynki" to feature produktu ze Slice 1, NIE rusztowanie makiety). Sekcja „Pomoc" = LATER (potwierdź z userem, że NIE w tym slice; hinty makiety = materiał źródłowy, zaparkowane).
7. **Cięcie zakresu.** To potencjalnie największy slice UI (881+694 linii do restrukturyzacji + 3 nowe elementy). Opcje: jeden slice (roadmap tak traktuje) vs dwa (11a: wizard+gating+dekompozycja; 11b: per-pole badge+panel+ekran rozbieżności). Rekomendacja i decyzja usera na checkpoincie (a).

**CO JUŻ ISTNIEJE (nie wyważać otwartych drzwi):** komponenty sekcji (subject/kw/inspection) — dekomponuj, nie przepisuj; guardy sekwencji (`fetchSeq`/`kwSeq`/`mapSeq`) — zachowaj przy przenoszeniu; `approvalGate` + blockery + confirm-akcje (serwerowe, wizard ich NIE zastępuje); `areaMismatch` nudge; grupowe badge na detalu; RTL harness per sekcja (`rtl-kw-section`, `rtl-inspection-section`, `rtl-features-section` renderuje CAŁY `<NewValuationForm/>` — restrukturyzacja formularza ZŁAMIE te testy, zaplanuj ich migrację w planie, nie ad-hoc); e2e smoke (network-free, flagi). **CZEGO NIE MA:** stepper/routing krokowy, per-pole badge, panel „Skąd te dane", ekran rozbieżności, edycja draftu po create (poza confirm/inspection), komponenty shadcn pod wizard (tabs/dialog/popover).

**KLUCZOWE ZASADY (jak w poprzednich slice'ach):**

- Kod/commity = ANGIELSKI (conventional, ≤100 znaków, lowercase, bez atrybucji); UI/operat = POLSKI (pełne diakrytyki).
- **F-1 NIETYKALNE**: golden 1 044 400; silnik KCS bez zmian. **F-4 NIETYKALNE**: `approvalGate` serwerowy — gating UI to warstwa NAD nim. **F-7**: mutacje draftu w tx z audytem (nowe mutacje edycji kroków = przemyśl akcje audytu; wzorzec `inspection_updated`). **F-12/F-9**: szablon nietykany w tym slice (UI-only); fixture'y syntetyczne, adresy tylko golden-case + fikcyjne (advisor sprawdza plan pod F-9!).
- Spec (S2) MUSI otwierać się sekcją „Opis produktowy — co budujemy z perspektywy użytkownika".
- **UI z makiety, nie z głowy** (`references/ui-planning.md`): każdy task UI wskazuje ekran/krok makiety; zrzuty w `raw/interactive-mockup/Wyceny - v2 - full code/screenshots/`.
- Per task (web): `pnpm turbo lint typecheck test build --env-mode=loose && pnpm depcruise` → commit → push → `gh run list --branch main --limit 3 --json databaseId,headSha` → `gh run watch <id> --exit-status` z TWOIM sha. Prettier pre-commit: `pnpm exec prettier --write`.
- **Hook guard (lekcja S10):** subagenci pushują PLAIN `git push`; jeśli PreToolUse guard zablokuje — subagent NIE obchodzi, zostawia commit lokalnie i raportuje; push robi kontroler.
- RTL: pragma `// @vitest-environment jsdom` + `afterEach(cleanup)`; web testy BEZ `clearMocks` — `.findLast()` na mock.calls; automocki `_deps`: `storage.get` resolwuje `undefined` (guard `Buffer.isBuffer`). CodeGraph przed grepem. Framework API (Next App Router routing kroków, shadcn) przez context7.
- Advisor-review planu przed checkpointem (b) — łapał blockery w KAŻDYM slice (S10: depcruise lib→adapters type-only; route miniatur, bez którego miniatury 404).
- E2e w CI network-free; smoke NAWIGUJE po formularzu — restrukturyzacja na kroki go złamie: zaplanuj aktualizację e2e w tym samym tasku co przełączenie routingu (kill-switch? stary formularz za flagą przejściowo? — do brainstormu/planu; lekcja „kill-switche shipuj WCZEŚNIE").
- QA przeglądarkowe (lekcje S9/S10): claude-in-chrome traci attach → fallback chrome-devtools (osobne cookies — sprawdź KIM jesteś; na /login SĄ przyciski demo-loginu bez hasła); **syntetyczne `fill`/`form_input` NIE stawia Reactowego state** — natywny setter + event `input`/`change` albo realne klawisze; natywny `<select>` przez setter+`change`.
- Weryfikacja prod: `railway run --service Postgres -- sh -c 'psql "$DATABASE_PUBLIC_URL" ...'`.

**STAN INFRA (2026-07-22 wieczór):** app repo `Raysharrr/wyceny` (`~/Development/wyceny-app`), main NIEchroniony, HEAD `08df2aa` (Slice 10 complete: kod `5bebaea..08df2aa`, CI green). Wiki repo `make-it-simple-rayshar/wyceny`, main CHRONIONY (PR, merge user), HEAD `b2d12a7` (PR #15 zmergowany). Prod: web https://wyceny-mu.vercel.app (deployment `wyceny-3n7fjimrb`), worker `worker-v2` Railway z Pillow (**deploy TYLKO: `railway up ./apps/worker --path-as-root --service worker-v2`** — ale ten slice workera NIE dotyka), Postgres (ostatnia migracja **0009**; wizard raczej zero DDL). CI e2e flagi off: `NEXT_PUBLIC_SUBJECT_AUTOFETCH`, `NEXT_PUBLIC_KW_UPLOAD`, `MAPS_FETCH`, `NEXT_PUBLIC_PHOTO_UPLOAD`. Demo: przyciski demo-loginu na /login (aneta=admin, zenon=rzeczoznawca). **Wyceny QA — NIE RUSZAĆ:** podpisane `5faecc25` (S8), `f9af0aba` (S9), `11e60dde` (S10, ze zdjęciami); drafty `e2cac945` (v2 S8), `3c813f0e` (v2 S10, z notatką — test wersjonowania zdjęć).

**KONTEKST RÓWNOLEGŁY:** telemetria „% pól as proposed" została w NEXT (user wybrał wizard merge'em #15); sesja z Anetą DOPIERO po pierwszej wersji MVP (pytania zbierane w `wiki/deliverables/pytania-do-anety.md`); backlog „photo hardening" ze Slice 10 (EOI-trailing-bytes, MAX_IMAGE_PIXELS, Cache-Control miniatur, walidacja section w remove, rename mintKwUploadToken, InspectionOp do inspection.ts — lista w ledgerze `# SLICE 10`) — **NIE brać do tego slice'a** (UI-only), chyba że user jawnie zdecyduje inaczej. Po wizardzie do MVP zostaje telemetria — wtedy prawdopodobnie sesja z Anetą.

**CHECKPOINTY:** (a) zakres po brainstormie (w tym decyzja: jeden slice czy cięcie na 11a/11b), (b) plan (po advisor-review), (c) deploy GO (web-only; zero nowych sekretów/migracji spodziewane), (d) merge wiki-PR, (e) commity w wiki repo za zgodą. Między checkpointami — w pełni autonomicznie.

**START:** potwierdź merge #15 i NOW w roadmapie → przeczytaj kontekst (1-6; makietę KLIKNIJ w przeglądarce) → `superpowers:brainstorming` (S1) — pytania jedna na raz, zaczynając od rozstrzygnięcia 1 (gating twardy AC-5 vs miękki z makiety).
