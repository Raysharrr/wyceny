# Handoff — Slice 12: UI wizard — parity WIZUALNE z makietą v3-r4 — start od S1 brainstorm

Wklej poniższy blok w **nowej sesji Claude Code** uruchomionej w `/Users/michalczekala/Development/wyceny` (wiki repo — CLAUDE.md kontrakt + skille). Handoff STARTOWY: slice **czysto prezentacyjny** (restyling wizarda z 11a do wyglądu makiety) — **bez spike'a** (zero zewnętrznych API), **zero zmian domeny/serwera/workera/szablonu/DDL**. Sesja prowadzi pełny cykl `/build-slice`.

---

Poprowadź **Slice 12 — UI wizard: parity WIZUALNE z makietą v3-r4** przez pełny cykl `/build-slice`: S1 brainstorm (⛔ checkpoint zakresu) → S2 spec+plan (⛔ checkpoint planu, advisor-review przed) → S3 `superpowers:subagent-driven-development` (świeży implementer + niezależny reviewer per task, commit+push+CI per task, ledger `.superpowers/sdd/progress.md` — NOWA sekcja `# SLICE 12`, briefy `parity-task-N-brief.md`/`-report.md`) → S4 CI/fitness → S5 deploy (⛔ — UWAGA: deploy jest teraz AUTOMATYCZNY, patrz niżej) → S6 wiki docs (PR, merge robi user).

**NAJPIERW:** potwierdź stan wiki-repo: PR **#16 ZMERGOWANY** (squash `9254f43`, 2026-07-23 — roadmap NOW = „UI wizard: parity WIZUALNE z makietą v3-r4"); PR **#17** (S7 — lekcje 11a w referencjach skilla build-slice) — sprawdź czy zmergowany; jeśli nie, poproś usera o merge (lekcje z #17 są istotne dla TEGO slice'a). Jeśli NOW jest inne — STOP, zapytaj usera.

**KONTEKST DO PRZECZYTANIA (w tej kolejności):**

1. `wiki/roadmap.md` — blok NOW (pełny zakres z parity punch-listy 2026-07-23: stepper + nagłówek kroku „KROK N/7 — …" TAKŻE na stronie create; layout kart-sekcji w 2 kolumnach ze sticky sidebar'ami [krok 1: mapa; krok 3: statystyki próby/Tabela 2; kroki 4–5: live ΣUi + podgląd WR]; dolny FootNav-bar z podsumowaniem pośrodku; topbar brand+user; AutoBanner statusowy kroku 3. **Jedyne trwałe odstępstwo od makiety [decyzja usera]: podgląd dokumentu = natywny viewer PDF, NIE inline A4.** Teksty edukacyjne makiety NADAL pomijane [FR-13]. DoD = side-by-side kroków 1–7 na prodzie bez rozjazdów layoutowych, zero regresji smoke/RTL, golden F-1 nietknięty, zero zmian F-4/F-7).
2. `wiki/topics/tech/wizard-ui-slice.md` — stan po Slice 11a: co zbudowane (lifecycle szkicu, mutacje, inwalidacja WR), **parity punch-lista** (sekcja „Parity: strukturalne TAK, wizualne NIE"), backlog (m.in. fast-follow: 1-linijkowa asercja linku „Pobierz DOCX" w smoke — dobry kandydat na task 0 tego slice'a).
3. **Makieta (źródło prawdy UI — reguła `references/ui-planning.md`, ROZSZERZONA w PR #17 o obowiązkowe side-by-side PRZED deployem):**
   - kod JSX: `raw/interactive-mockup/Wyceny - v2 - full code/` — **`shared.jsx`** (topbar, `Stepper` L146-177, `Card` L180-196, `AutoBanner` L199-208, `FootNav` L211-224, `MarkingLegend`, tokeny CSS `--ink-*`/`--accent`/`--amber` w stylach), **`screens-1.jsx`** (krok 1: layout 2-kolumnowy, sticky sidebar z mapą L125-143), **`screen2.jsx`** (krok 3: tabela kandydatów + sidebar „Statystyki próby"), **`screen3.jsx`** (krok 4: macierz cech + sidebar „Wskaźnik korekty ΣUi"+„Podgląd wartości (WR)"), **`screens-4-5.jsx`** (kroki 5-6), **`screen6.jsx`** (krok 7), **`screen-ogledziny.jsx`** (krok 2). Folder „Wyceny - v3 - full code" = ślepa odnoga — ignoruj;
   - klikalna: `raw/interactive-mockup/Wyceny - Makieta MVP (standalone) - v3-r4-2026-06-30.html` — **serwuj przez `python3 -m http.server 8788` w katalogu makiety** (rozszerzenie przeglądarki odmawia `file://`); login → lista → „ul. Kościelna 33/36" = wejście do wizarda.
4. App-repo `~/Development/wyceny-app` (CodeGraph zindeksowany — `codegraph explore` zamiast grep) — obecny stan UI po 11a:
   - `apps/web/src/app/valuations/[id]/page.tsx` — switch kroków (branch wizarda: draft+owner; flat view dla approved/signed/admin-cudzy), `stepper.tsx` (Stepper + WizardNav), `cards.tsx` (KcsBreakdown/karty — reużywane przez flat view i kroki), `steps/step-*.tsx` (inspection/sample/features/calculation/descriptions/operat + confirm-calculation-button);
   - `apps/web/src/app/valuations/new/page.tsx` + `subject-form.tsx` (krok 1 create+edit; BEZ steppera — to główny rozjazd do naprawy) + `subject-section.tsx`/`kw-section.tsx`;
   - shadcn zainstalowane (9): badge, button, card, checkbox, field, input, label, separator, table — doinstalowanie (jeśli brainstorm uzna) przez `pnpm dlx shadcn@latest add` w plan-tasku; framework API przez context7.

**ROZSTRZYGNIĘCIA JUŻ PODJĘTE (nie re-otwierać):** zakres = blok NOW (jak wyżej); odstępstwo PDF-viewer; FR-13 (bez tekstów edukacyjnych); zero zmian domeny/serwera; prowenancja per pole (dawne „11b": badge 🤖/✍️, panel „Skąd te dane", rozbieżności inline) = NEXT, **NIE w tym slice** (spec sekcja 11b w `docs/superpowers/specs/2026-07-22-wizard-ui-fr13-design.md` czeka).

**ROZSTRZYGNIĘCIA DO BRAINSTORMU (S1, z userem — jedna kwestia na raz):**

1. **Deploy-safety przy auto-CD (NAJPIERW — determinuje rytm S3).** Git-CD Vercela NAPRAWIONE 2026-07-23 (`apps/web/vercel.json`: turbo build + `--env-mode=loose`; commity `e30eb61`+`5638746`) — **KAŻDY push na main auto-deployuje PROD w ~50 s**. Restyling per task = częściowo przestylowany wizard na prodzie między taskami. Opcje: (a) akceptujemy (jeden user-tester, zmiany addytywne wizualnie); (b) flaga stylu na czas slice'a; (c) branch roboczy + jeden merge na koniec (łamie rytm push-per-task na main). Rekomendacja do dyskusji: (a) — najprościej, prod i tak oglądają tylko Dembscy w demo.
2. **Architektura shella.** Wspólny `WizardShell` (topbar + stepper + nagłówek kroku + FootNav jako layout z children/slotami) vs dokładanie do obecnego switcha w page.tsx. Jak wpiąć stepper na `/valuations/new` (create nie ma `valuationId` — stepper z krokiem 1 aktywnym i 2-7 disabled, linki puste?). Czy topbar wchodzi też na listę/login (globalny layout), czy tylko wizard.
3. **Sidebar'y per krok — skąd dane.** Krok 1: mapa — istniejący `mapPreview` z `subject-form` (dziś renderowany gdzie? sprawdź) + kafelek adres/pow.; krok 3: statystyki live z form state (Cmin/Cmax/Cśr już liczone — L185-197 wzorzec); kroki 4-5: **live ΣUi + podgląd WR wymaga `computeKcs` client-side na live form state** — silnik jest pure TS w `@/domain/kcs` (import do klienta legalny — F-10 to reguła zależności, nie server-only; ALE zweryfikuj depcruise i rozmiar bundle; alternatywa: licz tylko ΣUi lokalnie prostą sumą, WR-preview zostaw krokowi 5 jak dziś).
4. **Tokeny stylu.** Makieta ma własne CSS vars (`--ink-*`, `--accent` zielony, `--amber`, mono-numeryka `num`). Przenosić 1:1 jako theme (globals.css) vs mapować na istniejące tokeny shadcn (primary już zielony). Ryzyko: restyling globalny dotknie flat view i listę — pożądane (spójność) czy scope creep?
5. **FootNav sticky a per-step submity.** Makieta: jeden dolny bar (Wstecz | podsumowanie | primary action). Dziś: przyciski inline per krok (submit form kroków 3/4, button kroku 5, linki 2/6). FootNav jako komponent ze slotem na primary action (przekazywany przez krok) — jak to pogodzić z formami RHF (przycisk poza `<form>` → atrybut `form=` na buttonie albo portal?). Do planu: konkretny mechanizm.
6. **AutoBanner kroku 3** („Pobrano N transakcji z RCN…"): dane z `sampleMeta` (fetchedAt/query.count) — co dokładnie pokazać; czy banner też na kroku 1 po auto-fetchu przedmiotu (makieta go ma — `AutoBanner` w screens-1)?
7. **Zakres migracji testów RTL.** Restyling zmieni DOM — które selektory RTL są kruche (getByRole/label powinny przeżyć; screenshoty nie istnieją). Smoke: etykiety przycisków MUSZĄ zostać niezmienione („Dane się zgadzają — dalej" itd.) — inaczej task migruje smoke w tym samym commicie.

**CO JUŻ ISTNIEJE (nie wyważać otwartych drzwi):** cała logika kroków/mutacji/gatingu z 11a — **NIE DOTYKAĆ niczego poza JSX/klasami/kompozycją**; `Stepper`/`WizardNav` w `stepper.tsx` (restyle, nie rewrite); `cards.tsx` (KcsBreakdown = gotowa treść dla sidebar kroku 5); statystyki próby liczone w `step-sample.tsx`; `mapPreview` w subject-form. **CZEGO NIE MA:** topbar, nagłówki kroków, layout 2-kolumnowy, sticky sidebar'y, FootNav, AutoBannery, stepper na create.

**KLUCZOWE ZASADY (jak w poprzednich slice'ach):**

- Kod/commity = ANGIELSKI (conventional, ≤100 znaków, lowercase, bez atrybucji); UI = POLSKI (pełne diakrytyki).
- **F-1 NIETYKALNE** (golden 1 044 400); **F-4/F-7 NIETYKALNE** (zero zmian approvalGate/mutacji/audytu); **F-12** szablon nietykany; worker nietykany; **F-9** fixture'y syntetyczne.
- Spec (S2) MUSI otwierać się sekcją „Opis produktowy — co budujemy z perspektywy użytkownika".
- **UI z makiety, nie z głowy** + **side-by-side z klikalną makietą PRZED checkpointem deployu** (nowa reguła w `references/ui-planning.md` po PR #17 — ten slice jest jej pierwszym pełnym klientem; parity punch-lista → decyzja usera).
- Per task (web): `pnpm turbo lint typecheck test build --env-mode=loose && pnpm depcruise` → commit → push → `gh run list --branch main --limit 3 --json databaseId,headSha` → `gh run watch <id> --exit-status` z TWOIM sha. Prettier pre-commit: `pnpm exec prettier --write`. **Znany flake CI: job e2e umiera czasem timeoutem 15 min na „Install Playwright browsers" → `gh run rerun <id> --failed`.**
- **AUTO-DEPLOY:** push na main = deploy PROD (~50 s, alias przestawia się sam). NIE ma już kroku `vercel --prod`. Weryfikacja deployu = `vercel ls` (najnowszy Ready) + smoke-check aliasu. Checkpoint (c) deployu zmienia sens — ustal w brainstormie (rozstrzygnięcie 1).
- **Hook guard:** subagenci pushują PLAIN `git push`; jeśli PreToolUse guard zablokuje — subagent NIE obchodzi, zostawia commit lokalnie i raportuje; push robi kontroler.
- RTL: pragma `// @vitest-environment jsdom` + `afterEach(cleanup)`; BEZ `clearMocks` (`.findLast()` na mock.calls). **Pliki `"use server"` eksportują TYLKO async funkcje** (lekcja 11a — schematy w `wizard-schemas.ts`; naruszenie 500-uje strony i unit-gate tego NIE łapie). CodeGraph przed grepem. Framework API przez context7.
- Advisor-review planu przed checkpointem (b) — łapał blockery w KAŻDYM slice (11a: 3 blockery, m.in. crash SSR niewidoczny dla CI).
- QA przeglądarkowe: claude-in-chrome traci attach → fallback chrome-devtools (osobne cookies; na /login SĄ przyciski demo-loginu); **syntetyczny `fill`/`click` NIE stawia Reactowego state/handlerów** — natywny setter + event `input`/`change`, a kliki przez realne CDP click (lekcja 11a: syntetyczny `.click()` na przycisku approve nie triggernął akcji); makietę serwuj przez http.server.
- Weryfikacja prod DB: `railway run --service Postgres -- sh -c 'psql "$DATABASE_PUBLIC_URL" ...'`.

**STAN INFRA (2026-07-23 wieczór):** app repo `Raysharrr/wyceny` (`~/Development/wyceny-app`), main NIEchroniony, HEAD `5638746` (Slice 11a: `c117ced..a98ecc5` + fix git-CD `e30eb61`+`5638746`), CI green (sprawdź ostatni run na `5638746` — przy handoffie jeszcze się liczył). Prod: web https://wyceny-mu.vercel.app (**auto-deploy z main**, deployment `wyceny-8kg62pl50`), worker `worker-v2` Railway (nietykany; deploy TYLKO `railway up ./apps/worker --path-as-root --service worker-v2`), Postgres (ostatnia migracja **0010** — wizard; ten slice zero DDL). Wiki repo `make-it-simple-rayshar/wyceny`, main CHRONIONY (PR, merge user), HEAD `9254f43` (PR #16 merged), **PR #17 (S7) — sprawdź stan**. Demo: przyciski demo-loginu na /login (aneta=admin, zenon=rzeczoznawca). **Wyceny QA — NIE RUSZAĆ:** podpisane `5faecc25` (S8), `f9af0aba` (S9), `11e60dde` (S10, ze zdjęciami); drafty `e2cac945` (v2 S8), `3c813f0e` (v2 S10 — otwiera się w wizardzie na kroku 7, zdjęcia na kroku 2); **approved QA 11a: `7c99d991`** (846 300 zł, pełny ślad audytu wizarda z inwalidacją — dobry obiekt do side-by-side flat view).

**KONTEKST RÓWNOLEGŁY:** po tym slice w NEXT czekają: prowenancja per pole (spec gotowy — sekcja 11b w `2026-07-22-wizard-ui-fr13-design.md`) i telemetria „% pól as proposed"; sesja z Anetą po pierwszej wersji MVP (pytania w `wiki/deliverables/pytania-do-anety.md`); backlogi: „photo hardening" (ledger `# SLICE 10`), housekeeping 11a (martwe eksporty `assignProvenance`/`withComputedWr`, komentarze „Mirrors new-valuation-form"), fast-follow smoke DOCX-link (ledger `# SLICE 11A`), 16 podatności dependabota w wiki-repo — **NIE brać do slice'a bez decyzji usera** (poza ewentualnym task 0 = fast-follow smoke, 1 linia).

**CHECKPOINTY:** (a) zakres po brainstormie (w tym decyzja deploy-safety przy auto-CD), (b) plan (po advisor-review), (c) wg rozstrzygnięcia 1, (d) merge wiki-PR, (e) commity w wiki repo za zgodą. Między checkpointami — w pełni autonomicznie.

**START:** potwierdź stan #17 i NOW w roadmapie → przeczytaj kontekst (1-4; makietę KLIKNIJ przez http.server, side-by-side z prodem od razu na start — punch-lista z 11a jest w tech page, zweryfikuj że aktualna) → `superpowers:brainstorming` (S1) — pytania jedna na raz, zaczynając od rozstrzygnięcia 1 (deploy-safety przy auto-CD).
