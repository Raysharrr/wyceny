# Combined local acceptance — T06–T09

Coordinator worktree `pairwise-valuation`, application commit `3261551` (S3 `ad9f0c6`, S4 `c075dbb`), web3015/worker8015/PostgreSQL5544. Synthetic Zenon, isolated database `pairwise_valuation_test`, active Opisy and keyless real LibreOffice worker. No staging or main changes. Browser actions used Chrome through CUA; document inspection used the actual stored outputs and Poppler.

## Manual PP and new-version regression

- Ownership PP: `98667646-b692-4bac-9488-892248a15a8b`,50m², prices9000/9500/10000, custom feature100%, three-level subjectbetter/comparablesworse/average/better. Corrections1000/500/0 give500000. All six manual sections accepted; actual PDF preview rendered in Chrome, then approved and signed with the synthetic wave fixture.
- Same-value correction: multiplier0.5→0.50001 with explicit reason leaves rounded500000 (unrounded500000.17), makes only justification stale, preserves all six manual texts and five current sections. Justification manually revised/reconfirmed before issue. Actual signed PDF page9 shows four tables, precise override reason, the calculation and500000.17→500000 with the full-precision note; readable layout.
- New version: `adfd0fa5-69d7-43d3-b49d-35109748e3ec` correctly references the signed parent, requires method confirmation and retains rows/features/texts. Explicit KCS confirmation plus nine additional rows gives the full9000…10100 sample. Existing KCS rounding gives Vmax1.058,9550×1.058×50=505195→505200; no PP-style rounding was imposed on KCS. Analysis and justification become stale after method/sample change; four general texts retained. Both dependent texts revised, all six confirmed. Real PDF issued and signed; old signed parent unchanged.
- All four issued/signed DOCX texts and normalized PDF texts match. All six stored manual sections occur in each signed PDF. Signed PDF/DOCX SHA match the signed audit record. New-version link and signed state are present, only new-version action remains.

Evidence directory `/tmp/pairwise-integration-browser/`: browser snapshots, Chrome preview screenshot, issued/signed documents, `modern-documents-evidence.json`, PP page9 raster. Artifact data stays outside Git. The temporary export script reads only the four synthetic IDs and guards the isolated DB port/name.

## Cooperative PP and KCS — completed on the same application code

PP `c38b0a56-77d6-4e9f-8bff-eb7a92b115c0`: no KW,50m², five prices9000/10000/9000/10000/9000, one custom two-level feature100%, subjectbetter and alternating worse/better comparable ratings. Suggested multipliers1/0/1/0/1 produce five corrected prices10000 and500000. All six manual sections accepted, actual PDF issued and signed. Page9 visually checked: five readable columns, correct right and four tables.

New KCS version `f32f8d57-b3e0-47e0-93cc-d5e113a817c9`: method reconfirmed, seven added prices9100/9200/9300/9400/9500/9600/10100, twelve rows retained. Existing KCS arithmetic uses roundedVmax1.071 and produces505155→505200. All six texts revised/confirmed for the new version, PDF issued/signed. Page10 visually checked: middle coefficient and middle total are dashes, explanatory note visible, correct cooperative right/value. Both PP parents retain their original signed SHA after child creation/signature. `modern-documents-evidence.json` now covers all four signed modern documents, six texts, before/after text equality and audit SHA.

## Other acceptance evidence and boundaries

S1 approvals signed under S4 cover actual across-version legacy behavior for both rights; see S4-REVIEW and `/tmp/pairwise-s4-coordinator-browser/legacy-sign-evidence.json`. S3 manual form/Enter/stale tests and S4 all10 real PDF variants remain separately recorded. S5 owns the durable four-path KCS/PP×right suite and3× repetition; coordinator execution of that final suite is recorded below when integrated. This manual record covers two fresh PP drafts and two KCS new versions, not four fresh drafts, paid AI output, external imports, staging or deployment.

## Final durable run on coordinator stack

After S4 Linux correction and S5 merge, coordinator fetched origin and confirmed origin/main was already included (9b2903c). Application head `8b87be86b2ff5b5efff299ffc635f20827f7f9aa`. Restarted only the verified coordinator web3015/worker8015 processes from this worktree; isolated DB5544 unchanged. Both health/login succeeded. Keyless worker, active manual Opisy, actual local LibreOffice.

The now-integrated durable `e2e:pairwise` suite passed all8 executions in1.2min, workers2, no retries: four fresh method/right lifecycles, thresholds/reorder, custom scales/overrides/stale forms/prose, PP4 and Help. This complements the earlier manual new-version checks and independent Linux triplicate. `/tmp/pairwise-integration-final-e2e.log`, all trace/PDF/DOCX/screenshots under `/tmp/pairwise-integration-final-results/`. The previous prospective sentence is closed by this executed record. No paid generation, hosted database, production import or staging was involved.
