# Handoff — Slice 10: Oględziny — zdjęcia + notatka (FR-2) — start od S1 brainstorm

Wklej poniższy blok w **nowej sesji Claude Code** uruchomionej w `/Users/michalczekala/Development/wyceny` (wiki repo — CLAUDE.md kontrakt + skille). Handoff STARTOWY: slice ma gotową koncepcję (wiki §5.2 strony obrazów) i WSZYSTKIE wzorce techniczne dowiedzione w Slice 6+9 — sesja prowadzi pełny cykl. **Bez spike'a** (zero niezwalidowanych zewnętrznych API — precedens Slice 7); ewentualny mikro-spike Pillow EXIF tylko jeśli brainstorm uzna za konieczny.

---

Poprowadź **Slice 10 — Oględziny: zdjęcia + notatka (FR-2)** przez pełny cykl `/build-slice`: S1 brainstorm (⛔ checkpoint zakresu) → S2 spec+plan (⛔ checkpoint planu, advisor-review przed) → S3 `superpowers:subagent-driven-development` (świeży implementer + niezależny reviewer per task, commit+push+CI per task, ledger `.superpowers/sdd/progress.md` — NOWA sekcja `# SLICE 10`, briefy `ogledziny-task-N-brief.md`/`-report.md`) → S4 CI/fitness → S5 deploy (⛔) → S6 wiki docs (PR, merge robi user).

**NAJPIERW:** potwierdź, że wiki-PR **#14 jest ZMERGOWANY** (squash `0a35810`, 2026-07-22) i `wiki/roadmap.md` NOW = „Oględziny: zdjęcia + notatka (FR-2)". Jeśli NOW jest inne — STOP, zapytaj usera.

**KONTEKST DO PRZECZYTANIA (w tej kolejności):**

1. `wiki/roadmap.md` — blok NOW (outcome: 3 sekcje zdjęć — budynek zewn./wewn., otoczenie — z realnym uploadem + notatka z oględzin; sekcja foto przestaje być stubem; `Must-Viable`; jedyny z założenia ręczny krok w AI-first).
2. `wiki/topics/tech/obrazy-w-operacie-koncepcja.md` **§5.2** — ścieżka zdjęć: upload wzorcem KW/aktu → skalowanie (np. max 1600 px dłuższy bok) → **EXIF strip GPS+metadane urządzenia (RODO, Pillow/piexif)** → osadzenie tym samym mechanizmem co mapy. Ten slice definiuje model danych zdjęć.
3. `wiki/topics/tech/mapy-wms-slice.md` (Slice 9) — **mechanizm osadzania obrazów jest PRODUKCYJNY**: moduł image z per-tag sizing (`getImage(tagValue, tagName)`), sekcje warunkowe w szablonie (wzorzec `{#mapy}`, keepNext, tag obrazu NIGDY w jednym `w:t` z tagami sekcji), noga medialna F-12 (media+magic bytes+relacje — test tekstowy jest ślepy na obrazy), DEFLATE, klucze bytea w `document`, `PortStorage.delete` + typowany `StorageNotFoundError` w sign.
4. `wiki/topics/tech/kw-akt-ekstrakcja-slice.md` (Slice 6) — **wzorzec uploadu jest PRODUKCYJNY**: plik z przeglądarki WPROST do workera (limit 4,5 MB Vercela ominięty), token HMAC TTL 300 s (`WORKER_SHARED_SECRET`), CORS per origin, kill-switch `NEXT_PUBLIC_KW_UPLOAD`.
5. `wiki/topics/tech/immutability-audit-sign-slice.md` (Slice 8) — kontrakt modułu image (tagValue MUSI być stringiem, Buffer=crash, null=pusto), sign = re-render zamrożonych `inputs`, strażnik dryfu tekstu, audit w tx.
6. App-repo `~/Development/wyceny-app`: `apps/web/src/adapters/docx-render.ts` (opts `{signature?, maps?}` — zdjęcia = trzeci wpis, przemyśl rozszerzenie na `Record` per-sekcja), `apps/web/src/app/actions/approve-valuation.ts`/`sign-valuation.ts` (wzorzec odczytu zamrożonych bajtów przy renderze), `apps/worker/app/kw.py` + `main.py` `/kw-extract` (wzorzec endpointu z tokenem HMAC — do skopiowania dla uploadu zdjęć), `apps/web/src/app/valuations/new/kw-section.tsx` (wzorzec UI uploadu), wiki-repo `tools/spike/2026-07-15-template-koscielna/build_template.py` (etap map = wzorzec nowego etapu zdjęć).

**ROZSTRZYGNIĘCIA DO BRAINSTORMU (S1, z userem — jedna kwestia na raz):**

1. **Model danych zdjęć**: 3 sekcje (budynek zewn./wewn., otoczenie) — ile zdjęć per sekcja (limit górny? np. 3–6), formaty wejściowe (JPEG/PNG/HEIC?), max rozmiar pliku, rozdzielczość po resize (koncepcja: max 1600 px). Notatka z oględzin — jedno pole tekstowe czy per sekcja?
2. **Przechowywanie i determinizm**: rekomendacja — bajty po obróbce (resize+EXIF strip w workerze) do tabeli `document` (klucze np. `ogledziny-{sekcja}-{n}-{valuationId}.jpg`, wzorzec map); upload w DRAFCIE = zapis od razu (dane wprowadzone ręcznie jak KW, nie fetch przy approve); render approve i sign czytają te same bajty → determinizm za darmo. UWAGA na lekcję finałowego review Slice 9: osierocone klucze (usunięte zdjęcie, nieudany upload) NIE mogą przeciekać do renderu — projektuj cykl życia kluczy (delete przy usunięciu/nadpisaniu).
3. **EXIF strip**: zawsze (GPS + metadane urządzenia), worker-side przy uploadzie. Sprawdź czy Pillow jest już zależnością workera (jeśli nie — nowa zależność, Dockerfile bez zmian poza uv.lock?). Czy zachować orientację (EXIF Orientation → transpose przed stripem!).
4. **Prowenancja i potwierdzenie**: zdjęcia = wprowadzone przez rzeczoznawcę (jak `manual`) — czy osobny confirm („Potwierdź dokumentację z oględzin") pod bramę F-4, czy bez bramki? Notatka — pole w `inputs`?
5. **Miejsce w operacie**: gdzie sekcje zdjęć (§8.1 „Dokumentacja fotograficzna" — stub istnieje; §8.3 opis budynku; §16 załączniki?) i podpisy pod zdjęciami (własne opisy rzeczoznawcy czy stałe etykiety sekcji?). Nowy etap `build_template.py` z sekcjami warunkowymi per sekcja zdjęć.
6. **Uczciwa cisza vs blocker**: operat bez zdjęć — renderuje stub jak dziś (rekomendacja: honest silence, zdjęcia „wymagane w MVP" = zdolność produktu, nie twardy blocker każdej wyceny) czy approve blokowany? Interakcja z `inspectionDate` (pole już istnieje).
7. **(Opcjonalnie) follow-up backlogu Slice 9 jako task 0**: `mapSeq` guard na fire-and-forget podglądu map (stale-last-wins pokazuje mapy poprzedniego adresu; fix trywialny, precedens `fetchSeq` w tym samym pliku) + duplikat zdania stubu w §8.1 bez map (poprawka copy w builderze — i tak dotykasz szablonu w tym slice). Rekomendacja: wziąć oba.

**CO JUŻ ISTNIEJE (nie wyważać otwartych drzwi):** upload HMAC wzorcem Slice 6 (token, CORS, kill-switch); moduł image + per-tag sizing + sekcje warunkowe + noga medialna F-12 + DEFLATE (Slice 9); `document` bytea + `PortStorage.get/put/delete` + `StorageNotFoundError`; `inspectionDate` w modelu i formularzu; audit `meta` jsonb (zero migracji dla flag); `AbortSignal.timeout` w adapterach. **CZEGO NIE MA:** model danych zdjęć, endpoint uploadu zdjęć w workerze, obróbka obrazu (resize/EXIF — prawdopodobnie brak Pillow w workerze, sprawdź `pyproject.toml`), tagi zdjęć w szablonie, UI sekcji oględzin, notatka z oględzin w `inputs`.

**KLUCZOWE ZASADY (jak w poprzednich slice'ach):**

- Kod/commity = ANGIELSKI (conventional, ≤100 znaków, lowercase, bez atrybucji); UI/operat = POLSKI (pełne diakrytyki).
- **F-1 NIETYKALNE**: golden 1 044 400; syntetyczne fixture'y (zdjęcia testowe = generowane 1×1/małe syntetyczne, ZERO realnych fotografii); KW TYLKO `PO1P/1/6`; zero 11-cyfrowych ciągów (uwaga na base64 w fixture'ach — advisor sprawdza plan pod F-9!).
- **F-12**: szablon WYŁĄCZNIE przez `build_template.py` (wiki repo, zmiana UNCOMMITTED do S6 PR); NBSP przez Python I/O; noga medialna rozszerzona o zdjęcia (liczba mediów per wariant!). **F-11**: worker nie zwraca WR. **F-7 NIETYKALNE**: triggery bez zmian, każda mutacja z audytem w tx, zamknięta lista `AUDIT_ACTIONS` (nowe akcje = przemyśl czy meta na istniejącej wystarczy — wzorzec `mapsSkipped`).
- **RODO**: EXIF strip zawsze; zdjęcia wnętrz mogą być daną osobową — bajty tylko w Postgresie (wzorzec skanu podpisu); zero retencji plików tymczasowych w workerze.
- Spec (S2) MUSI otwierać się sekcją „Opis produktowy — co budujemy z perspektywy użytkownika".
- Per task (web): `pnpm turbo lint typecheck test build --env-mode=loose && pnpm depcruise` → commit → push → `gh run list --branch main --limit 3 --json databaseId,headSha` → watch runu z TWOIM sha (`gh run watch <id> --exit-status`). Worker: `cd apps/worker && uv run pytest -q && uv run ruff check .`. Prettier pre-commit: `pnpm exec prettier --write`.
- RTL: pragma `// @vitest-environment jsdom` + `*.test.tsx`; harness z `rtl-kw-section.test.tsx`. UWAGA: web testy NIE mają `clearMocks` — mock.calls akumulują się (`.findLast()`). Automocki `_deps`: `storage.get` resolwuje `undefined` zamiast rzucać (guard `Buffer.isBuffer` — lekcja B2 Slice 9). CodeGraph przed grepem (app-repo zindeksowany). Framework API przez context7.
- Advisor-review planu przed checkpointem (b) — łapał blockery w KAŻDYM z ostatnich slice'ów (w tym: sam plik planu łamiący F-9, e2e czerwone w połowie planu — kill-switche shipuj WCZEŚNIE).
- E2e w CI jest network-free: nowe flagi wzorcem `MAPS_FETCH=off` / `NEXT_PUBLIC_KW_UPLOAD=off` (upload zdjęć = klientowy flag `NEXT_PUBLIC_*`, bo przyciski renderuje klient).
- QA przeglądarkowe: claude-in-chrome może stracić attach → **fallback chrome-devtools MCP** (osobna sesja cookie — sprawdź KIM jesteś zalogowany: w Slice 9 fallback był na anecie i owner-only przyciski „znikały"); syntetyczne `form_input` NIE triggeruje RHF blur — realne klawisze.
- Weryfikacja bajtowa artefaktów prod: `railway run --service Postgres -- sh -c 'psql "$DATABASE_PUBLIC_URL" ...'` (base64 dump `content_bytes`) — wzorzec z QA Slice 9.

**STAN INFRA (2026-07-22):** app repo `Raysharrr/wyceny` (`~/Development/wyceny-app`), main NIEchroniony, HEAD `144f243`; CI joby `ci`+`e2e` (flagi off: autofetch, kw-upload, maps). Wiki repo `make-it-simple-rayshar/wyceny`, main CHRONIONY (PR, merge user), HEAD `0a35810` (PR #14 zmergowany). Prod: web https://wyceny-mu.vercel.app (deployment `wyceny-cszc32gjp`), worker `worker-v2` Railway — **deploy: `railway up ./apps/worker --path-as-root --service worker-v2`** (bez `--path-as-root` railpack FAIL na root monorepo!), Postgres Railway (ostatnia migracja **0009**; Slice 9 bez DDL — zdjęcia też raczej zero DDL jeśli notatka pójdzie w `inputs`). Demo: aneta=admin (`Admin123!`), zenon=rzeczoznawca (`Rzeczoznawca123!`). **Wyceny QA — NIE RUSZAĆ**: podpisane `5faecc25-...` (Slice 8) i `f9af0aba-...` (Slice 9, z mapami), draft v2 `e2cac945-...`.

**KONTEKST RÓWNOLEGŁY:** pytania do Anety (`wiki/deliverables/pytania-do-anety.md`, 10 pozycji; H4/M5 z adnotacjami decyzji tymczasowych) — **decyzja usera 2026-07-22: sesja z Anetą DOPIERO po pierwszej wersji MVP** (skrócenie ping-ponga) — pytania zbierać dalej, nie planować sesji w tym slice. Po Oględzinach do MVP zostają: **UI wizard parity (FR-13)** i telemetria „% pól as proposed" (roadmap NEXT) — przy S6 promocja NEXT→NOW = decyzja usera przy merge.

**CHECKPOINTY:** (a) zakres po brainstormie, (b) plan (po advisor-review), (c) deploy GO (worker dotknięty — nowy endpoint + ew. Pillow; sekret `WORKER_SHARED_SECRET` już istnieje, nowych raczej zero), (d) merge wiki-PR, (e) commity w wiki repo za zgodą. Między checkpointami — w pełni autonomicznie.

**START:** potwierdź merge #14 i NOW w roadmapie → przeczytaj kontekst (1-6) → `superpowers:brainstorming` (S1) — pytania jedna na raz, zaczynając od rozstrzygnięcia 1 (model danych zdjęć).
